public class Main {
    public static void main(String[] args) {

        String description = "This calc operates with both Arabic and Roman numbers." +
                "Both operands should be of one kind, otherwise the app is terminated having thrown an exception." +
                "Same thing happens if the input is invalid due to either faulty operands or operators." +
                "The calc operates with ints in range from 1 to 10." +
                "The kind of input numbers defines the kind of the output.";
        System.out.println();

        System.out.println(calc);
    }

    public static String calc(String input){

        String result = "";
        System.out.println(result);
    }

}